"use client";
// import { IPost } from "@/types/post.type";

// { posts }: { posts: IPost[] }
export function ProfilePosts() {

  // const images = posts.map(post => post.image);

  return "ProfilePosts";
}
